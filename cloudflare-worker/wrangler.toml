name = "opensky-proxy"
main = "opensky-proxy.js"
compatibility_date = "2024-01-01"

[vars]
# Set these via `wrangler secret put` — NOT here
# OPENSKY_ID = "..."
# OPENSKY_SECRET = "..."
# PROXY_API_KEY = "..."  # optional: restrict access to your app
